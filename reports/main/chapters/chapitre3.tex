% ═══════════════════════════════════════════════════════════════════════════════
%  CHAPITRE 3 : ANALYSE DES BESOINS ET CONCEPTION DU SYSTÈME
% ═══════════════════════════════════════════════════════════════════════════════
\newpage
\section{Analyse des besoins et conception du système}
\label{ch3}

\subsection{Introduction}
\label{ch3:intro}

Ce chapitre détaille la phase de conception du système de gestion des soutenances. Il définit les exigences fonctionnelles et non fonctionnelles, formalise les règles de gestion qui régissent la planification, et présente la modélisation \acrshort{uml} du système. Cette étape est cruciale pour s'assurer que la solution technique répond précisément aux besoins du département.

\subsection{Présentation générale du projet}
\label{ch3:presentation_generale}

Le projet consiste en la réalisation d'une application web centralisée permettant l'organisation complète des sessions de soutenances. Contrairement à une simple gestion d'agenda, le système agit comme un moteur de planification intelligent capable d'orchestrer des sessions globales (normale, rattrapage) avec des paramètres configurables incluant la durée des soutenances, les pauses entre les passages, et les coefficients d'évaluation par rôle.

Le système couvre l'ensemble du cycle de vie d'une soutenance : de la création des projets et la constitution des jurys, en passant par la planification interactive des créneaux via un éditeur visuel par glisser-déposer (\textit{Defense Designer}), jusqu'à la saisie des évaluations et la génération automatique des documents administratifs (convocations, procès-verbaux, fiches d'évaluation). Chaque étape est validée par un moteur de détection de conflits préservant l'intégrité du planning.

Les inclus dans le périmètre du projet : la gestion des utilisateurs et des rôles, la configuration académique (filières, niveaux, salles), la gestion des projets et des groupes d'étudiants, la constitution des jurys, la planification interactive des créneaux, la détection automatique de conflits, la saisie des évaluations, la génération de documents et le système de notifications. Les exclus principales du périmètre : la publication des résultats finaux (bulletins), l'intégration avec le système d'information universitaire existant.

\subsection{Cahier des charges}
\label{ch3:cahier_charges}

\subsubsection{Acteurs du système}
\label{ch3:acteurs}

Le système repose sur une gestion stricte des accès basée sur les rôles (\textit{Role-Based Access Control — RBAC}). Quatre acteurs principaux sont identifiés, chacun disposant d'un périmètre fonctionnel spécifique. Le choix de quatre rôles uniquement, sans possibilité de superposition, simplifie considérablement la logique de contrôle d'accès tout en couvrant l'ensemble des besoins identifiés.

L'\textbf{Administrateur} est responsable de la configuration structurelle de l'établissement. Il gère les départements, filières, niveaux et salles. Il est également en charge de la création des comptes utilisateurs (import massif via fichiers Excel) et de la configuration des paramètres de défense. Ce rôle est le seul à pouvoir accéder aux journaux d'audit.

Le \textbf{Coordinateur} est l'acteur central du système. Il gère l'ensemble du cycle de planification : import et validation des projets, constitution des jurys selon les templates de rôles, utilisation du \textit{Defense Designer} pour la planification interactive des créneaux, consultation du tableau de bord des conflits, et suivi des évaluations. Il peut également publier le planning final et générer les documents administratifs. Ce rôle nécessite une vue d'ensemble de toutes les soutenances et une compréhension des contraintes de chaque acteur.

L'\textbf{Enseignant} est membre du corps professoral impliqué dans les soutenances. Il consulte son planning personnel, déclare ses indisponibilités via un calendrier interactif, consulte les rapports des projets qui lui sont assignés, saisit les notes des étudiants qui lui sont assignés, et accède à ses convocations. Son interaction avec le système est principalement consultative, à l'exception de la saisie des notes et de la déclaration d'indisponibilités.

L'\textbf{Étudiant} est l'utilisateur final du système. Il visualise les informations relatives à sa soutenance (date, salle, jury), crée ou rejoint un groupe de projet, et téléverse les documents requis (rapport, présentation, archive). Il peut également télécharger sa convocation officielle au format A4. Son périmètre d'action est le plus limité, ce qui est cohérent avec son rôle dans le processus de soutenance.

\Cref{tab:acteurs_use_cases} résume la répartition des fonctionnalités principales par acteur.

\begin{table}[H]
    \centering
    \begin{xltabular}{\textwidth}{lXXXX}
        \toprule

        \textbf{Fonctionnalité}               & \textbf{Admin} & \textbf{Coord.} & \textbf{Enseign.} & \textbf{Étudiant} \\
        \midrule
        Dashboard personnalisé                & \checkmark     & \checkmark      & \checkmark        & \checkmark        \\
        Gestion départements                  & \checkmark     & —               & —                 & —                 \\
        Gestion salles (+ import Excel)       & \checkmark     & —               & —                 & —                 \\
        Gestion utilisateurs (+ import Excel) & \checkmark     & —               & —                 & —                 \\
        Configuration filières/niveaux        & \checkmark     & —               & —                 & —                 \\
        Journal d'audit                       & \checkmark     & —               & —                 & —                 \\
        Gestion projets                       & —              & \checkmark      & —                 & —                 \\
        Gestion groupes étudiants             & —              & \checkmark      & —                 & —                 \\
        Constitution jurys                    & —              & \checkmark      & —                 & —                 \\
        Sessions de soutenance (cycle de vie) & —              & \checkmark      & —                 & —                 \\
        Planification créneaux (DnD)          & —              & \checkmark      & —                 & —                 \\
        Détection conflits                    & —              & \checkmark      & —                 & —                 \\
        Consultation notes/moyennes           & —              & \checkmark      & —                 & —                 \\
        Génération documents \acrshort{pdf}              & —              & \checkmark      & —                 & —                 \\
        Saisie indisponibilités               & —              & —               & \checkmark        & —                 \\
        Saisie évaluations                    & —              & —               & \checkmark        & —                 \\
        Consultation rapports                 & —              & —               & \checkmark        & —                 \\
        Consultation planning                 & —              & \checkmark      & \checkmark        & \checkmark        \\
        Création/adhésion groupe              & —              & —               & —                 & \checkmark        \\
        Téléverser documents de soutenance    & —              & —               & —                 & \checkmark        \\
        Téléchargement convocation            & —              & —               & —                 & \checkmark        \\
        \bottomrule
    \end{xltabular}
    \caption{Répartition des fonctionnalités par acteur}
    \label{tab:acteurs_use_cases}
\end{table}

\subsubsection{Besoins fonctionnels}
\label{ch3:besoins_fonctionnels}

Les besoins sont organisés par modules métier, chacun correspondant à un sous-système cohérent de l'application. Chaque module a été identifié lors de l'analyse du processus existant avec le coordinateur et l'encadrant, puis priorisé en fonction de son impact sur le processus global de soutenance.

L'\textbf{authentification} est assurée par le module Gestion Identité via \acrshort{jwt} avec tokens d'accès et de rafraîchissement. La récupération de mot de passe utilise un lien à durée limitée (1 heure), et l'activation de compte valide la force du mot de passe (bibliothèque zxcvbn). L'interface de connexion unifiée redirige automatiquement selon le rôle de l'utilisateur.

L'\textbf{administration des filières} (\acrshort{mip}, BCG, etc.) et des niveaux (Licence 3, Master 2, etc.) relève du module Gestion Académique, qui inclut l'import massif des étudiants et des salles via fichiers Excel avec validation des données et gestion des erreurs ligne par ligne. Le flux technique de cet import, depuis la sélection du fichier jusqu'à la persistance en base de données, est détaillé dans la \Cref{fig:seq_import_data}.
\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_import_data}
    \caption{Séquence d'importation massive des données depuis un fichier Excel}
    \label{fig:seq_import_data}
\end{figure}
Une interface \gls{crud} complète avec recherche et pagination permet de maintenir les référentiels à jour.

La \textbf{constitution des jurys} est gérée par le module Gestion des Projets et Jurys, qui suit les projets selon des statuts (proposé, approuvé, rejeté) et permet de constituer des jurys selon des templates configurables avec rôles normalisés (Président, Rapporteur, Examinateur). Le suivi de la charge des enseignants évite les sur-affectations.

Les \textbf{sessions de soutenance} sont gérées via un cycle de vie complet : Brouillon $\rightarrow$ Active $\rightarrow$ Planifiée $\rightarrow$ Terminée $\rightarrow$ Archivée. Chaque session paramètre la durée des soutenances, les pauses, les dates bornes et les coefficients d'évaluation par rôle de jury.

Le \textit{Defense Designer} est l'élément central du système. Cette interface interactive de type canevas permet au coordinateur de planifier les soutenances par glisser-déposer : les jurys, listés dans une barre latérale, sont déposés sur un calendrier dynamique organisé par salles et créneaux horaires. L'état local est maintenu côté client et synchronisé avec le serveur uniquement lors de la sauvegarde explicite. Un algorithme d'auto-génération peuple automatiquement le planning en optimisant l'occupation des salles.

En matière de \textbf{détection de conflits}, le moteur dédié valide systématiquement \metric{ReglesGestion} règles de gestion lors de chaque modification du planning (cf. \cref{ch3:regles_gestion}). Dupliquée côté client pour un retour instantané et exécutée côté serveur pour préserver l'intégrité lors de la sauvegarde, cette architecture double-validation combine réactivité et fiabilité.

La \textbf{saisie des notes} est pilotée par le module Évaluation, qui gère les coefficients dynamiques par session et effectue le calcul automatique de la moyenne pondérée. Le membre du jury peut consulter le rapport du projet assigné pour préparer son évaluation, puis saisir les notes. Le coordinateur peut consulter les notes moyennes pondérées par projet, avec le détail des scores par membre du jury.

L'\textbf{édition documentaire} produit \metric{TypesDocuments} types de documents \gls{pdf} (fiches d'évaluation, procès-verbaux, listes de présence, convocations jury, planning complet) générés côté serveur et ouverts dans un nouvel onglet du navigateur. Le module Notifications et Audit complète le dispositif avec des notifications internes (success, warning, error) et la journalisation des actions critiques pour la traçabilité.

Enfin, la gestion des \textbf{groupes d'étudiants} permet aux étudiants de créer ou rejoindre un groupe pendant une fenêtre temporelle définie, et aux coordinateurs d'associer les groupes aux projets.

Le module \textbf{documents étudiants} gère le téléversement de fichiers (rapport, présentation, archive) avec respect des délais et période configurables par le coordinateur.

\subsubsection{Besoins non fonctionnels}
\label{ch3:besoins_non_fonctionnels}

Les besoins non fonctionnels définissent les qualités transverses du système, souvent aussi critiques que les fonctionnalités elles-mêmes pour assurer l'adoption et la satisfaction des utilisateurs.

La \textbf{réactivité} constitue un besoin fondamental pour le \textit{Defense Designer} : le retour sur conflit lors du glisser-déposer doit être instantané (latence $< 100$ms) grâce à la validation locale via le moteur de conflits TypeScript. L'état du planning est maintenu dans la mémoire du navigateur, sans aucun appel réseau pendant les opérations de glisser-déposer.

La \textbf{sécurité} repose sur le chiffrement des mots de passe via \texttt{BCrypt}, la protection des endpoints par tokens \acrshort{jwt} avec validation côté serveur (Spring Security), et le contrôle d'accès basé sur les rôles via annotations \texttt{@PreAuthorize}. Pour prévenir les attaques de type XSS (\textit{Cross-Site Scripting}), les tokens sont transmis et stockés via des cookies \texttt{httpOnly}, les rendant inaccessibles aux scripts JavaScript du navigateur. Des protections contre les attaques par injection \acrshort{sql}, les failles XSS et les erreurs d'autorisation horizontale ont été renforcées.

Côté \textbf{disponibilité}, une architecture \textit{stateless} du backend permet un déploiement horizontal scalable. L'\acrshort{api} \acrshort{rest} ne maintient pas d'état de session côté serveur : chaque requête est autonome via le token \acrshort{jwt}, ce qui facilite tests et débogage.

L'\textbf{ergonomie} s'appuie sur une interface moderne et responsive (\texttt{Tailwind \acrshort{css} 4}, \texttt{shadcn/ui}). La navigation est contextualisée par rôle avec une sidebar adaptative, et le thème clair/sombre avec persistance du choix améliore le confort lors de sessions prolongées.

La \textbf{maintenabilité} résulte d'une architecture en couches stricte (Contrôleur $\rightarrow$ Service $\rightarrow$ Repository $\rightarrow$ Entité) avec séparation claire des responsabilités. Les \texttt{DTOs} isolent les couches \acrshort{api} et \texttt{MapStruct} gère le mapping objet, protégeant le modèle interne des variations de l'interface exposée.

Enfin, la \textbf{qualité} est mesurée par une couverture minimale de \metric{CouvertureLignesBackCible} (lignes) et \metric{CouvertureBranchesBackCible} (branches) via \texttt{JaCoCo}, complétée par un linting automatisé avec \texttt{ESLint} (TypeScript), et \texttt{Spotless}, \texttt{Checkstyle}, \texttt{PMD} et \texttt{SpotBugs} (Java) intégré dans CI.

\subsection{Règles de gestion}
\label{ch3:regles_gestion}

Le moteur de conflits implémente les règles de gestion (RG) suivantes, maintenant l'intégrité du planning.

\begin{projectbox}{Règles de Gestion du Planning}
    \begin{description}
        \item[RG1 (Enseignants)] Un enseignant ne peut être affecté à deux soutenances dont les horaires se chevauchent.
        \item[RG2 (Salles)] Une salle ne peut accueillir qu'une seule soutenance pour un créneau donné.
        \item[RG3 (Période)] Toutes les soutenances d'une session doivent être planifiées entre la \texttt{startDate} et la \texttt{endDate} de ladite session.
        \item[RG4 (Encadrants)] Un encadrant ne doit pas être sollicité sur plusieurs projets le même jour pour éviter la surcharge.
        \item[RG5 (Unicité)] Un projet ne peut être planifié qu'une seule fois par session.
        \item[RG6 (Disponibilité)] Aucune soutenance ne peut être planifiée sur un créneau déclaré comme indisponible par un membre du jury.
    \end{description}
\end{projectbox}

Ces \metric{ReglesGestion} règles sont implémentées dans deux moteurs distincts mais fonctionnellement identiques : un moteur Java côté serveur (\texttt{ConflictDetectionService}) qui valide lors de la sauvegarde, et un moteur TypeScript côté client (\texttt{conflict-engine.ts}) qui fournit un retour instantané lors des interactions dans le \textit{Defense Designer}.

\Cref{tab:rg_mapping} associe chaque règle à son ou ses implémentations et à la sévérité de la violation. Les règles classées « error » bloquent l'opération, tandis que les règles classées « warning » affichent une alerte mais autorisent l'enregistrement. RG4 est classée en « warning » plutôt qu'« error » car la surcharge d'un encadrant, bien qu'indésirable, ne compromet pas l'intégrité du planning : un encadrant peut superviser plusieurs projets dans la même journée. Le coordinateur conserve la liberté de déroger à cette règle selon le contexte.

\begin{table}[H]
    \centering
    \small
    \begin{xltabular}{\textwidth}{lXccc}
        \toprule

        \textbf{Règle} & \textbf{Description}     & \textbf{Java} & \textbf{TypeScript} & \textbf{Sévérité} \\
        \midrule
        RG1            & Chevauchement enseignant & \checkmark    & \checkmark          & error             \\
        RG2            & Occupation de salle      & \checkmark    & \checkmark          & error             \\
        RG3            & Bornes de période        & \checkmark    & \checkmark          & error             \\
        RG4            & Surcharge encadrant      & \checkmark    & \checkmark          & warning           \\
        RG5            & Unicité de projet        & \checkmark    & \checkmark          & error             \\
        RG6            & Indisponibilité          & \checkmark    & \checkmark          & error             \\
        \bottomrule
    \end{xltabular}
    \caption{Correspondance entre les règles de gestion et leurs implémentations}
    \label{tab:rg_mapping}
\end{table}

Lorsqu'une règle ne peut être satisfaite (par exemple, aucun créneau disponible pour un jury donné), le système ne propose pas de mécanisme d'override automatique. Le coordinateur doit soit modifier les paramètres de la session (dates, salles), soit reconfigurer la composition du jury, soit déclarer une indisponibilité compensatoire. Cette approche conservative préserve l'intégrité du planning au prix d'une flexibilité réduite, un choix délibéré compte tenu du caractère critique des contraintes académiques.

\subsection{Conception \acrshort{uml}}
\label{ch3:conception_uml}

La modélisation \acrshort{uml} permet de formaliser les différentes visions du système : qui fait quoi (cas d'utilisation), comment les données sont structurées (classes), comment les composants interagissent (séquence), et comment les processus se déroulent (activité). Nous avons utilisé les diagrammes \acrshort{uml} 2.5 dans tous les diagrammes.

\begin{projectbox}{Remarque}
    Afin de garantir une lisibilité optimale, quelques diagrammes ont été scindés par acteur, notamment diagrammes de cas d'utilisation. Bien qu'un diagramme unique regroupant l'ensemble des acteurs et des fonctionnalités soit la norme théorique, la complexité du système rendait une telle approche illisible.
\end{projectbox}

\subsubsection{Diagrammes de cas d'utilisation}
\label{ch3:uc}

Les diagrammes de cas d'utilisation délimitent les responsabilités de chaque acteur. Ils ont été construits à partir de l'analyse du processus existant, puis validés lors des revues de conception.

\Cref{fig:uc_hierarchy} présente la hiérarchie générale des acteurs. L'acteur de base « Utilisateur » est spécialisé en quatre rôles distincts, chacun héritant de la capacité de s'authentifier. Cette hiérarchie simple reflète l'architecture RBAC du système, où chaque rôle correspond à un ensemble disjoint de permissions.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{uc_hierarchy}
    \caption{Hiérarchie générale des acteurs}
    \label{fig:uc_hierarchy}
\end{figure}

\Cref{fig:uc_admin} détaille les cas d'utilisation de l'Administrateur. Ce rôle est le seul à pouvoir configurer les structures académiques (départements, filières, salles), gérer les comptes utilisateurs (création, modification, suppression, import massif) et consulter les journaux d'audit.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{uc_admin}
    \caption{Cas d'utilisation : Administration du système}
    \label{fig:uc_admin}
\end{figure}

\Cref{fig:uc_coordination} détaille les cas d'utilisation du Coordinateur, l'acteur le plus sollicité par le système. Il gère le cycle complet : des projets et groupes d'étudiants, à la constitution des jurys, en passant par la planification interactive via le \textit{Defense Designer}, la validation du planning, et la génération des documents. Les relations « include » et « extend » modélisent les dépendances entre cas d'utilisation (par exemple, « Affecter un jury » inclut « Vérifier les conflits »).

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{uc_coordination}
    \caption{Cas d'utilisation : Coordination et \textit{Defense Designer}}
    \label{fig:uc_coordination}
\end{figure}

\Cref{fig:uc_jury} présente les cas d'utilisation de l'Enseignant en tant que membre de jury. Il consulte son planning personnel, déclare ses indisponibilités via un calendrier interactif, consulte le rapport du projet assigné, saisit les notes et observations pour les étudiants qui lui sont assignés, et accède à ses convocations. Le cas « Saisir les évaluations » inclut la consultation des informations de l'étudiant, afin que le jury dispose du contexte nécessaire avant de noter.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{uc_jury}
    \caption{Cas d'utilisation : Enseignant / Membre de jury}
    \label{fig:uc_jury}
\end{figure}

\Cref{fig:uc_student} présente les cas d'utilisation de l'Étudiant. Son périmètre est le plus restreint : il consulte les informations relatives à sa soutenance (date, salle, jury), télécharge sa convocation, et gère son groupe de projet (création, recherche de coéquipiers). Le cas « Télécharger la convocation » inclut la consultation des informations de soutenance.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{uc_student}
    \caption{Cas d'utilisation : Étudiant}
    \label{fig:uc_student}
\end{figure}

\subsubsection{Diagramme de classes}
\label{ch3:classes}

Plusieurs choix de conception structurels ont été adoptés pour répondre aux besoins du métier. La composition du jury est modélisée comme une relation enrichie entre une soutenance et un enseignant, permettant d'associer un rôle spécifique (Président, Rapporteur, Examinateur) à chaque membre sans complexifier la structure des entités. Afin de garantir l'intégrité historique des données, les évaluations sont liées aux enseignants de manière à ce que les notes et observations soient préservées même en cas de modification ultérieure de la composition du jury. Enfin, la flexibilité de la notation est assurée par un système de pondération dynamique associé à chaque session, permettant d'ajuster les coefficients de calcul des notes selon le rôle du membre du jury.

Le diagramme de classes présenté en \Cref{fig:class_global} traduit la structure des données du système. On y observe une hiérarchie d'utilisateurs basée sur la spécialisation, permettant de distinguer les responsabilités de chaque rôle tout en centralisant les informations communes.

\begin{figure}[H]
    \centering
    \pumlsvg[0.7\textwidth]{class_global}
    \caption{Diagramme de classes global du système}
    \label{fig:class_global}
\end{figure}

\subsubsection{Diagrammes de séquence}
\label{ch3:sequence}

Les diagrammes de séquence modélisent les interactions entre les composants du système pour des cas d'utilisation critiques. Nous en présentons six qui illustrent les flux les plus complexes.

\Cref{fig:seq_jury_assignment} modélise le processus de constitution des jurys, détaillant les interactions entre le coordinateur, le service de gestion des jurys et la base de données pour assurer une répartition équitable des membres.
\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_jury_assignment}
    \caption{Séquence d'affectation et de validation des membres du jury}
    \label{fig:seq_jury_assignment}
\end{figure}

\Cref{fig:seq_planning} modélise le flux de planification interactive via le \textit{Defense Designer}. Ce processus repose sur un cycle de validation itératif où le frontend et le backend collaborent pour préserver l'absence de conflits :

\begin{enumerate}
    \item L'utilisateur fait glisser un jury depuis la barre latérale vers une cellule du calendrier.
    \item Le hook \texttt{useDefenseSchedule} construit le contexte (\texttt{ConflictContext}) à partir des données locales.
    \item Le moteur TypeScript (\texttt{validateSlotAssignment}) exécute les six vérifications.
    \item En cas d'erreur, un message est affiché avec une suggestion de résolution ; le dépôt est annulé.
    \item En cas de succès, l'état local (\texttt{schedule}) est mis à jour immédiatement (\textit{optimistic update}).
    \item Lors de la sauvegarde, les données sont envoyées au serveur qui ré-exécute les vérifications via \texttt{ConflictDetectionService} avant persistance.
\end{enumerate}

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_planning}
    \caption{Séquence de planification interactive via le \textit{Defense Designer}}
    \label{fig:seq_planning}
\end{figure}
La \Cref{fig:seq_validate_planning} illustre le flux de validation globale du planning, où le coordinateur verrouille la session pour empêcher toute modification ultérieure et déclenche la génération des documents officiels.
\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_validate_planning}
    \caption{Séquence de validation globale et verrouillage de la session}
    \label{fig:seq_validate_planning}
\end{figure}
L'algorithme d'auto-génération est détaillé dans la \Cref{fig:seq_auto_generation}, montrant comment le système tente de placer chaque projet sur le premier créneau libre disponible en respectant les contraintes de capacité et les règles de gestion.
\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_auto_generation}
    \caption{Séquence de l'algorithme d'auto-génération du planning}
    \label{fig:seq_auto_generation}
\end{figure}

\Cref{fig:seq_auth} modélise le flux d'authentification. L'utilisateur soumet ses identifiants via le formulaire de connexion, le frontend envoie une requête POST à \texttt{/api/auth/login}, le serveur valide les identifiants (vérification du hash BCrypt et du statut du compte), puis retourne un token \acrshort{jwt} via un cookie \texttt{httpOnly} sécurisé. Le navigateur gère alors automatiquement l'envoi de ce cookie lors des requêtes suivantes, et le frontend redirige l'utilisateur vers la page correspondant à son rôle.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_auth}
    \caption{Séquence d'authentification via \acrshort{jwt}}
    \label{fig:seq_auth}
\end{figure}

\Cref{fig:seq_evaluation} modélise le flux de soumission d'une évaluation. L'enseignant saisit le score et le commentaire pour un étudiant, le frontend envoie une requête POST à l'endpoint d'évaluation, le serveur valide la note (plage 0--20), enregistre l'évaluation, met à jour le statut (PENDING $\rightarrow$ SUBMITTED), et retourne la confirmation. Le moteur de notation calculera ensuite la moyenne pondérée une fois toutes les évaluations d'un jury soumises.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{seq_evaluation}
    \caption{Séquence de soumission d'une évaluation}
    \label{fig:seq_evaluation}
\end{figure}

\subsubsection{Diagrammes d'activité}
\label{ch3:activite}

L'algorithme de détection des conflits, moteur central du système, est modélisé par le diagramme d'activité de la \cref{fig:act_conflict}. Chaque vérification est une étape indépendante, permettant d'identifier l'ensemble des violations en un seul passage. L'algorithme exécute les \metric{ReglesGestion} règles de gestion en séquence et collecte toutes les violations détectées avant de retourner le résultat à l'interface.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{act_conflict_logic}
    \caption{Logique de validation des \metric{ReglesGestion} règles de gestion (RG1-RG6)}
    \label{fig:act_conflict}
\end{figure}
Le flux opérationnel global du coordinateur lors de la phase de planification est synthétisé dans le diagramme d'activité de la \Cref{fig:act_planning}, mettant en évidence les boucles de correction et les points de décision.
\begin{figure}[H]
    \centering
    \pumlsvg[0.6\textwidth]{act_planning}
    \caption{Diagramme d'activité du processus de planification}
    \label{fig:act_planning}
\end{figure}

\Cref{fig:act_evaluation} modélise le processus d'évaluation d'une soutenance. Chaque membre du jury saisit individuellement sa note (sur 20), ses observations et son statut de présence via l'endpoint dédié. La soumission est définitive : une fois enregistrée, la note est verrouillée. Une fois tous les membres ayant soumis, le système calcule automatiquement la moyenne pondérée en appliquant les coefficients configurés par rôle de jury.

\begin{figure}[H]
    \centering
    \pumlsvg[0.8\textwidth]{act_evaluation}
    \caption{Processus d'évaluation et de notation d'une soutenance}
    \label{fig:act_evaluation}
\end{figure}

\subsection{Architecture logicielle}
\label{ch3:architecture_logicielle}

Le système adopte une architecture découplée pour maximiser la maintenabilité et la testabilité. La séparation frontend/backend via une \acrshort{api} \acrshort{rest} permet à chaque couche d'évoluer indépendamment.

\Cref{fig:component_architecture} présente la vue composants du système, organisée en trois couches principales : le frontend React, le backend Spring Boot et la base de données.

\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{component_architecture}
    \caption{Architecture en composants du système}
    \label{fig:component_architecture}
\end{figure}

\subsubsection{Backend (\acrshort{api} \acrshort{rest})}
Le backend suit un pattern en couches strict : \textbf{Contrôleur $\rightarrow$ Service $\rightarrow$ Repository $\rightarrow$ Entité}. Cette structure permet d'isoler la logique métier (notamment le \texttt{ConflictDetectionService}) de la couche d'accès aux données. Les DTOs assurent le découplage entre les \acrshort{api} exposées et le modèle interne. Le projet est organisé en \metric{PackagesBackend} packages fonctionnels :

\begin{itemize}[label=\textcolor{primary}{\textbullet}]
    \item \texttt{auth/} -- authentification \acrshort{jwt}, mot de passe oublié, activation de compte
    \item \texttt{admin/} -- gestion des entités académiques (salles, départements, filières, configuration, stats)
    \item \texttt{coordinator/} -- cœur métier (projets, groupes, jurys, planification, conflits, soutenances, documents, notes, stats)
    \item \texttt{teacher/} -- evaluations, indisponibilités, planning personnel
    \item \texttt{student/} -- groupe, documents, convocation, statistiques
    \item \texttt{notification/} -- système de notifications, email et événements
    \item \texttt{common/} -- configuration Spring Security, exceptions, mappers, audit, génération \acrshort{pdf}
    \item \texttt{seed/} -- initialisation des données de démonstration
    \item \texttt{user/} -- hiérarchie des utilisateurs (User, Teacher, Student, Coordinator)
\end{itemize}

\subsubsection{Frontend (SPA)}
L'interface est conçue comme une application monopage (\textit{Single Page Application}) utilisant React 19 avec TypeScript 6 en mode strict. L'état asynchrone est géré par \texttt{TanStack Query v5}, qui assure la synchronisation avec l'\acrshort{api}, la mise en cache et le rechargement automatique. Le \textit{Defense Designer} implémente un état local optimiste pour une fluidité maximale lors du glisser-déposer, grâce à la bibliothèque \texttt{@dnd-kit/core}. Chaque page est chargée via \texttt{React.lazy()} pour optimiser le temps de chargement initial, et les routes sont protégées par des gardes \texttt{ProtectedRoute} qui vérifient le rôle de l'utilisateur avant d'autoriser l'accès.

\subsubsection{Sécurité et contrôle d'accès}
La sécurité repose sur une architecture \acrshort{jwt} \textit{stateless} avec contrôle d'accès par annotations \texttt{@PreAuthorize}. L'implémentation détaillée est présentée au \cref{ch4:module_auth}.

\subsection{Modélisation de la base de données}
\label{ch3:mld}

Le Modèle Logique de Données (MLD) traduit les classes \acrshort{jpa} en tables relationnelles. La base de données \texttt{H2} en développement utilise le même schéma que \texttt{\acrshort{mysql}} en production, garantissant la compatibilité entre les environnements.

L'héritage des utilisateurs est implémenté via la stratégie \texttt{JOINED} : la table \texttt{users} contient les attributs communs (email, password, role, firstName, lastName, isActive), tandis que les tables \texttt{teachers}, \texttt{students} et \texttt{coordinators} stockent les attributs spécifiques. Cette stratégie, plus coûteuse en jointures que la stratégie \texttt{SINGLE\_TABLE}, a été choisie pour sa clarté sémantique et sa facilité d'évolution.

Les données flexibles sont gérées via les annotations \texttt{@ElementCollection} : les membres d'un jury (\texttt{defense\_members}), les créneaux d'indisponibilité (\texttt{unavailability\_slots}), les coefficients d'évaluation par session (\texttt{defense\_session\_coefficients}), et les rôles d'un template de jury (\texttt{template\_roles}). Cette approche, bien que moins performante que des tables séparées pour les données volumineuses, est adaptée au volume de données attendu dans un contexte universitaire.

Les entités de configuration (\texttt{DefenseSettings}) utilisent un pattern singleton garantissant l'unicité de la configuration globale.

La structure relationnelle finale, incluant la gestion de l'héritage des utilisateurs et les collections d'éléments pour les données flexibles, est représentée dans le \Cref{fig:mld_schema}.
\begin{figure}[H]
    \centering
    \pumlsvg[\textwidth]{mld_schema}
    \caption{Modèle Logique de Données (MLD)}
    \label{fig:mld_schema}
\end{figure}

\subsection{Normes et standards respectés}
\label{ch3:normes}

Le développement du système suit des normes et standards industriels pour préserver la qualité, la maintenabilité et la cohérence du code source.

Côté Java, le code respecte les conventions de nommage Java (camelCase pour les méthodes et variables, PascalCase pour les classes), les directives de codage Google (limitation à 100 caractères par ligne, gestion des imports non utilisés), et les bonnes pratiques Spring (injection de dépendances par constructeur, annotations de validation sur les DTOs). Les noms de tables et de colonnes suivent la convention snake\_case en base de données, reflétée par les annotations \texttt{@Table} et \texttt{@Column}.

Côté TypeScript, le code suit les conventions Airbnb (formatage \texttt{Prettier}, règles \texttt{ESLint} strictes), le typage strict est activé (\texttt{strict: true} dans le \texttt{tsconfig.json}), et les noms de fichiers utilisent le kebab-case (\texttt{conflict-engine.ts}, \texttt{use-defense-schedule.ts}). Les composants React suivent le pattern fonctionnel avec hooks, sans classes.

Les conventions de message de commit et l'organisation des issues GitHub sont détaillées au Chapitre~4 (cf. \cref{ch4:git_conventions}).

\subsection{Conclusion}
\label{ch3:conclusion}

Ce chapitre a permis de formaliser les besoins du système et de concevoir une architecture capable de supporter la complexité de la planification académique. La définition rigoureuse des six règles de gestion, la modélisation \acrshort{uml} complète (cas d'utilisation, classes, séquence, activité) et la description de l'architecture logicielle en couches servent désormais de base technique pour l'implémentation détaillée qui sera présentée au chapitre suivant. Le respect des normes de codage et des standards industriels assure la maintenabilité du système à long terme.
